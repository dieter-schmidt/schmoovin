using UnityEngine;

namespace NeoFPS.ModularFirearms
{
    public enum SprintFireAction
    {
        CannotFire,
        StopSprinting,
        StopAnimation
    }

    public enum SprintInterruptAction
    {
        StopSprinting,
        StopAnimation
    }
}
